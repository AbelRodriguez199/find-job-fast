<%- layout('layouts/main') %>
  <h1 class="mt-4 text-center" ><u><b>MODIFICAR OFERTA</b></u></h1>
  <h3 class="mt-4 text-center " ><b>¡¡ ADVERTENCIAS !!</b></h3>
  <div class=" offset-md-3">
  <ol>
    <li>La Fecha no puede ser modificada</li>
    <li>Asegúrate que los cambios sean correctos antes de guardar</li>
  </ol>
</div>

  <div class="container">
    <div class="row">
      <div class="container">
        <div class="row">
      <div class="col-md-6 offset-md-3">
          <div class="card">
              <div class="card-body">
                  <form action="/update/<%= offer._id %>" method="POST">
                      <div class="form-group">
                          <input type="text" name="post" placeholder="¿Cual es el puesto?" class="form-control" value="<%= offer.post %>"></input>
                      </div>
                      <div class="form-group">
                          <textarea name="description" placeholder="Describe" cols="80" class="form-control"><%= offer.description %>
                          </textarea>
                      </div>
                      <div class="form-group">
                          <textarea name="requirements" placeholder="Requisitos" cols="80" class="form-control"><%= offer.requirements %></textarea>
                      </div>
                      <div class="form-group">
                          <input type="texto" name="salary" placeholder="Salario" class="form-control" value="<%= offer.salary %>">
                        </input>
                      <br>
                      <button type="submit" class="btn btn-primary btn-block">
                        Guardar Cambios
                      </button>
                  </form>
              </div>
          </div>
      </div>
      </div>
    </div>
    </div>
    </div>
  </div>